# mDNS-and-DNS-SD-Implementation
A demonstration application that implements Multicast DNS (mDNS) and DNS-Based Service Discovery (DNS-SD) using the socket module, without external networking libraries. The project simulates a service discovery system for resource monitoring in a local network.
